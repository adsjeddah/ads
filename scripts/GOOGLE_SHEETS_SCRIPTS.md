# سكريبتات Google Sheets

دليل شامل لجميع السكريبتات المتعلقة بنظام أرشفة Google Sheets.

## 📜 السكريبتات المتاحة

### 1. تهيئة Google Sheets

**الملف**: `initialize-google-sheets.js`

**الوظيفة**: إضافة عناوين الأعمدة وتنسيق الشيت (يُنفذ مرة واحدة)

**الاستخدام**:
```bash
node scripts/initialize-google-sheets.js
```

**ما يقوم به**:
- ✅ إضافة عناوين الأعمدة (A-J)
- ✅ تلوين صف العناوين (خلفية زرقاء، نص أبيض)
- ✅ تجميد الصف الأول
- ✅ محاذاة النص في المنتصف

**متى تستخدمه**:
- عند الإعداد الأولي للشيت
- إذا تم حذف العناوين بالخطأ
- لإعادة تنسيق صف العناوين

---

### 2. اختبار الاتصال

**الملف**: `test-google-sheets.js`

**الوظيفة**: اختبار الاتصال مع Google Sheets وإضافة صف تجريبي

**الاستخدام**:
```bash
node scripts/test-google-sheets.js
```

**ما يقوم به**:
- ✅ التحقق من صلاحيات الوصول
- ✅ قراءة عناوين الأعمدة
- ✅ إضافة صف تجريبي ببيانات وهمية
- ✅ التحقق من نجاح الإضافة

**متى تستخدمه**:
- للتأكد من عمل الاتصال مع Google Sheets
- عند مواجهة مشاكل في الحفظ التلقائي
- للتحقق من صلاحيات Service Account

---

### 3. عرض البيانات المحفوظة

**الملف**: `view-google-sheets-data.js`

**الوظيفة**: عرض جميع البيانات المحفوظة في الشيت بتنسيق منظم

**الاستخدام**:
```bash
node scripts/view-google-sheets-data.js
```

**ما يقوم به**:
- ✅ قراءة جميع البيانات من الشيت
- ✅ عرض عدد المعلنين المحفوظين
- ✅ عرض تفاصيل كل معلن بشكل منظم

**متى تستخدمه**:
- للمراجعة السريعة للبيانات المحفوظة
- للتحقق من نجاح الحفظ
- للحصول على نظرة عامة على الأرشيف

**مثال على المخرجات**:
```
📊 عدد المعلنين المحفوظين: 5

1. شركة النقل الممتاز (0501234567) - نقل العفش - جدة
   الباقة: 30 يوم | المدفوع: 500 ريال | المتبقي: 300 ريال
   من 25/11/2025 إلى 25/12/2025
```

---

## 🔄 سير العمل الموصى به

### الإعداد الأولي (مرة واحدة):

```bash
# 1. تهيئة العناوين والتنسيق
node scripts/initialize-google-sheets.js

# 2. اختبار الاتصال
node scripts/test-google-sheets.js

# 3. عرض البيانات للتأكد
node scripts/view-google-sheets-data.js
```

### الاستخدام اليومي:

لا حاجة لتشغيل أي سكريبتات! النظام يعمل تلقائياً عند:
- إضافة معلن جديد من لوحة التحكم
- إنشاء اشتراك جديد للمعلن

### المراقبة والمراجعة:

```bash
# عرض البيانات المحفوظة في أي وقت
node scripts/view-google-sheets-data.js
```

---

## 🛡️ الأمان

جميع السكريبتات تستخدم نفس بيانات الاعتماد:
- Service Account: `prokr-leads@review-saudimoving.iam.gserviceaccount.com`
- الصلاحيات: القراءة والكتابة في Spreadsheets فقط

---

## 📊 هيكل البيانات

```
A: تاريخ اضافة المعلن    (تاريخ الإنشاء)
B: اسم المعلن             (اسم الشركة)
C: رقم الهاتف            (رقم التواصل)
D: نوع الخدمة او القطاع   (نقل العفش، النظافة، إلخ)
E: المنطقة الجغرافية      (المملكة، جدة، الرياض، إلخ)
F: الباقة                (اسم الباقة ومدتها)
G: الدفعة المقدمة        (المبلغ المدفوع)
H: المتبقي               (المبلغ المتبقي)
I: بداية الاعلان         (تاريخ البداية)
J: انتهاء الاعلان        (تاريخ الانتهاء)
```

---

## 🐛 استكشاف الأخطاء

### خطأ: "Permission denied"

**السبب**: Service Account ليس لديه صلاحية الوصول للشيت

**الحل**:
1. افتح Google Sheets
2. اضغط "مشاركة"
3. أضف: `prokr-leads@review-saudimoving.iam.gserviceaccount.com`
4. امنحه صلاحية "محرر"

### خطأ: "Invalid credentials"

**السبب**: بيانات الاعتماد غير صحيحة

**الحل**:
1. تحقق من Private Key في السكريبتات
2. تأكد من عدم وجود أحرف مفقودة في المفتاح

### خطأ: "Range not found"

**السبب**: اسم الورقة غير صحيح

**الحل**:
- تأكد من أن اسم الورقة هو `Sheet1`
- أو عدّل `range` في السكريبتات

---

## 💡 نصائح

1. **النسخ الاحتياطي**: Google Sheets يحفظ التاريخ تلقائياً
2. **الفلترة**: استخدم فلاتر Google Sheets للبحث في البيانات
3. **التقارير**: أنشئ charts وتقارير مباشرة من الشيت
4. **التصدير**: يمكنك تصدير البيانات كـ Excel أو CSV

---

## 🔗 الروابط المفيدة

- [Google Sheets](https://docs.google.com/spreadsheets/d/1pN_pwoOuujKBIp4Ca04Z_fovuix5VMtJ3h4rFFY3wno/edit)
- [Google Sheets API Docs](https://developers.google.com/sheets/api)
- [Service Accounts Guide](https://cloud.google.com/iam/docs/service-accounts)

